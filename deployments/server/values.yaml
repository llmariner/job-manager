global:
  database:
    host:
    port:
    username:

  databaseSecret:
    name:
    key:

  ingress:
    ingressClassName:

  auth:
    enable: true
    rbacInternalServerAddr:

grpcPort: 8081
httpPort: 8080

# The following default values work if model-manager-server runs in the same namespace.
fileManagerServerAddr: llm-operator-file-manager-server-grpc:8081
modelManagerServerAddr: llm-operator-model-manager-server-grpc:8081

database:
  database: job_manager

jobNamespace:

debug:
  kubeconfigPath:

replicaCount: 1

serviceAccount:
  create: true
  serviceAccountName: job-manager-server

image:
  repository: public.ecr.aws/v8n3t7y5/llm-operator/job-manager-server
  pullPolicy: IfNotPresent


version:
